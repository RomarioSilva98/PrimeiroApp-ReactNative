import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: "100%",
    height: 52,
    backgroundColor: "#f8f8f8",
    borderRadius: 10,
    padding: 16,
    color: "#000",
    borderWidth: 1,
    borderColor: "#e0e0e0",
  }
});